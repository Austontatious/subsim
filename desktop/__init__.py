"""Desktop frontend scaffolding for SubSim.

Holds OS/IO specific components (windowing, input, audio) for
the desktop build. Existing modules `render`, `audio`, and `ui`
will remain the implementation; adapters can be added here to
conform to the abstract interfaces in `subsim.core.engine_api`.
"""

